# segredos e ambiente
.env
.venv/

# estado de execução (cookies de sessão + banco de dedup)
state/

# dados REAIS capturados (CNPJ/nomes de clientes, ofícios, screenshots) — NUNCA versionar.
# A pasta tests/fixtures/ só contém dumps reais; os testes sintéticos são inline no código.
tests/fixtures/

# caches / temporários
__pycache__/
*.pyc
.pytest_cache/
*.log
.DS_Store
